// #include<iostream>
// #include<cstdint> // for fixed type integers
// using namespace std;

// // since the int type varies from system to system based on the bit system of that system i.e. 64 bit or 32 bit system
// // but if we need to use fixed size type then c++ provides some of the fixed type data types, it holds the same number of bits irrespective of the system's bit system

//  /*
//   *
//   * int8_t : its size is 1byte so the range is -2^7 to 2^7 - 1 i.e. -128 to 127
//   * uint8_t : it is unsigned int8_t , its size is 1byte so the range is 0 to 2^8 - 1 i.e. 255
//   * int16_t : its size is 2byte so the range is -2^15 to 2^15 - 1
//   * uint16_t : size is 2byte so the range is 0 to 2^16 - 1
//   *  and similarly we have int32_t and int64_t and also their unsigned versions
//   */

// int main(){

//     // the int8_t and uint8_t typically behaves as char
//     int8_t val = 65;
//     std :: cout << val << endl; // this will print char 'A'

//     uint8_t val2 = 65;
//     cout << val2 << endl;

//     // rest others behave correctly
//     int16_t val1 = 65;
//     cout << val1 << endl;
// }

